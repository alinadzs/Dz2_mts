import java.time.LocalDate;

public class Cat extends Pet {
    public Cat(String name, double cost, String character, LocalDate birthDate) {
        super("Cat", name, cost, character, birthDate);
    }
}