import java.time.LocalDate;

public class Shark extends Predator {
    public Shark(String name, double cost, String character, LocalDate birthDate) {
        super("Shark", name, cost, character, birthDate);
    }
}