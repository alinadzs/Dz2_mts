import java.time.LocalDate;

public class Wolf extends Predator {
    public Wolf(String name, double cost, String character, LocalDate birthDate) {
        super("Wolf", name, cost, character, birthDate);
    }
}